pipeline {
    agent any
    environment {
      manifest_file = "ddkndksndksn"
      //BUILD_NUMBER = "${params.BUILD_NUMBER}"
    }
   stages {
    	stage("set version name"){
        	steps {
                	script {
                        parallel{
                            stage{
                                echo "$BUILD_NUMBER"                    
                            }
                            stage{
                                echo "$BUILD_NUMBER"                    
                            }
                            
                        }
                    }
            }
        }
            
            
            
            
            
   }
}
